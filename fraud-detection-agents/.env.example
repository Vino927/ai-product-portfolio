# Secret: inject through your deployment platform or shell.
# This application intentionally does not load .env files at runtime.
ANTHROPIC_API_KEY=

# Optional deployment-specific endpoint override.
ANTHROPIC_API_URL=https://api.anthropic.com/v1/messages
